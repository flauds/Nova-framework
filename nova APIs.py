import flask
print("Flask is installed and ready to use!")
import speech_recognition as sr

# Initialize the recognizer
recognizer = sr.Recognizer()

print("SpeechRecognition is installed and working!")
import speech_recognition as sr
recognizer = sr.Recognizer()
